# Builds the Angular frontend and deploys it to GitHub Pages. Talks to the
# backend cross-origin (see deploy.yml / deploy/README.md for that half).
# Publishes as a normal project page (https://nobuddy.org/RideMergeBuddy) -
# nobuddy.org is already the org's Pages custom domain, so no CNAME needed
# in this repo.
#
# Repo variables (Settings > Secrets and variables > Actions > Variables):
#   API_BASE_URL       - bare backend domain. Terraform computes and owns this
#                         (backend_subdomain + dns_zone_name in
#                         deploy/terraform/variables.tf) - defaults to
#                         activitymerger-api.nobuddy.org, so set this to match
#                         unless you changed those variables.
#   PAGES_CUSTOM_DOMAIN - leave unset for this repo (see above)
name: Deploy frontend to GitHub Pages

on:
  push:
    branches: [master]
    paths:
      - 'web-app/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '14'

      - name: Install dependencies
        working-directory: web-app
        run: npm ci

      - name: Point the build at the backend
        working-directory: web-app
        run: sed -i "s#__API_BASE_URL__#${API_BASE_URL}#" src/environments/environment.prod.ts
        env:
          API_BASE_URL: ${{ vars.API_BASE_URL }}

      - name: Determine base href
        id: basehref
        run: |
          if [ -n "${{ vars.PAGES_CUSTOM_DOMAIN }}" ]; then
            echo "value=/" >> "$GITHUB_OUTPUT"
          else
            echo "value=/${GITHUB_REPOSITORY#*/}/" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        working-directory: web-app
        run: npx ng build --base-href "${{ steps.basehref.outputs.value }}"

      - name: Add 404.html for deep-link/refresh support
        # GitHub Pages is a static host - a hard navigation or refresh on a
        # client-side route like /RideMergeBuddy/login has no matching file,
        # so Pages serves 404.html's content instead (while the browser
        # still shows the real URL). Making that a copy of index.html lets
        # Angular boot normally and its router pick up the correct route
        # from the actual address bar.
        run: cp web-app/dist/web-app/index.html web-app/dist/web-app/404.html

      - name: Add CNAME for custom domain
        if: vars.PAGES_CUSTOM_DOMAIN != ''
        run: echo "${{ vars.PAGES_CUSTOM_DOMAIN }}" > web-app/dist/web-app/CNAME

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web-app/dist/web-app

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
